# راهنمای مشاهده پروژه Paradik

## نحوه اجرا و مشاهده پروژه

### مرحله 1: اجرای سرور توسعه

در ترمینال PowerShell یا Command Prompt، دستور زیر را اجرا کنید:

```bash
npm run dev
```

یا اگر از PowerShell استفاده می‌کنید:

```powershell
npm run dev
```

### مرحله 2: باز کردن در مرورگر

بعد از اجرای دستور بالا، باید پیام زیر را ببینید:

```
  ▲ Next.js 15.x.x
  - Local:        http://localhost:3000
  - Network:      http://192.168.x.x:3000
```

### مرحله 3: مشاهده پروژه

1. مرورگر خود را باز کنید (Chrome، Firefox، Edge و غیره)
2. به آدرس زیر بروید:
   ```
   http://localhost:3000
   ```

### ویژگی‌های پروژه

✅ **هدر مدرن و حرفه‌ای**
   - نوار جستجوی پیشرفته با پیشنهادات
   - منوی ناوبری
   - دکمه‌های ورود و ثبت‌نام
   - سبد خرید

✅ **صفحه اصلی (Homepage)**
   - هیرو کاروسل
   - بخش دسته‌بندی‌ها
   - محصولات ویژه
   - تامین‌کنندگان
   - بخش RFQ

✅ **پشتیبانی RTL/LTR**
   - زبان فارسی (راست به چپ)
   - زبان انگلیسی (چپ به راست)
   - تغییر زبان از هدر

### دستورات مفید

```bash
# اجرای سرور توسعه
npm run dev

# ساخت پروژه برای production
npm run build

# اجرای نسخه production
npm run start

# بررسی خطاهای TypeScript
npm run type-check

# فرمت کردن کد
npm run format
```

### عیب‌یابی

**اگر سرور اجرا نشد:**

1. بررسی کنید Node.js نصب باشد:
   ```bash
   node --version
   ```
   باید نسخه 20 یا بالاتر باشد.

2. بررسی کنید npm نصب باشد:
   ```bash
   npm --version
   ```

3. اگر وابستگی‌ها نصب نیست، اجرا کنید:
   ```bash
   npm install
   ```

**اگر پورت 3000 اشغال است:**

می‌توانید پورت دیگری استفاده کنید:
```bash
npm run dev -- -p 3001
```

سپس به آدرس `http://localhost:3001` بروید.

### نکات مهم

- سرور توسعه باید همیشه در حال اجرا باشد
- تغییرات در فایل‌ها به صورت خودکار اعمال می‌شود (Hot Reload)
- برای توقف سرور، در ترمینال `Ctrl + C` را بزنید

---

**موفق باشید! 🚀**




