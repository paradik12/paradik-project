# پروژه پارادیک - پلتفرم B2B

یک پروژه React + Next.js برای سایت B2B با صفحات و کامپوننت‌های کامل.

## ویژگی‌ها

- ✨ صفحات کامل: Home, Products, Product Details, About, Contact
- 🎨 UI مدرن و زیبا با Tailwind CSS
- 📱 واکنش‌گرا (Responsive)
- 🔍 جستجو و فیلتر محصولات
- 🛠️ ساختار منظم با TypeScript
- 📦 خدمات API و توابع کمکی

## ساختار پروژه

```
paradik-project/
├── app/                    # صفحات Next.js (App Router)
│   ├── page.tsx           # صفحه اصلی
│   ├── products/          # صفحات محصولات
│   ├── about/             # صفحه درباره ما
│   └── contact/           # صفحه تماس
├── components/            # کامپوننت‌های React
│   ├── Header.tsx
│   ├── Footer.tsx
│   ├── ProductCard.tsx
│   ├── SearchBar.tsx
│   └── FilterPanel.tsx
├── services/              # سرویس‌های API
│   ├── productService.ts
│   └── apiClient.ts
└── utils/                 # توابع کمکی
    ├── format.ts
    ├── validation.ts
    └── constants.ts
```

## نصب و راه‌اندازی

1. نصب وابستگی‌ها:
```bash
npm install
```

2. اجرای پروژه در حالت توسعه:
```bash
npm run dev
```

3. باز کردن مرورگر در آدرس:
```
http://localhost:3000
```

## اسکریپت‌ها

- `npm run dev` - اجرای سرور توسعه
- `npm run build` - ساخت پروژه برای تولید
- `npm run start` - اجرای پروژه در حالت تولید
- `npm run lint` - بررسی کد با ESLint

## تکنولوژی‌ها

- **Next.js 14** - فریمورک React
- **TypeScript** - تایپ‌های استاتیک
- **Tailwind CSS** - استایل‌دهی
- **React 18** - کتابخانه UI

## صفحات

- **خانه** (`/`) - صفحه اصلی با معرفی و ویژگی‌ها
- **محصولات** (`/products`) - لیست محصولات با جستجو و فیلتر
- **جزئیات محصول** (`/products/[id]`) - صفحه جزئیات هر محصول
- **درباره ما** (`/about`) - اطلاعات شرکت
- **تماس با ما** (`/contact`) - فرم تماس و اطلاعات تماس

## کامپوننت‌ها

- **Header** - هدر سایت با منوی ناوبری
- **Footer** - فوتر با لینک‌ها و اطلاعات تماس
- **ProductCard** - کارت نمایش محصول
- **SearchBar** - نوار جستجو
- **FilterPanel** - پنل فیلتر محصولات

## خدمات

- **productService** - سرویس‌های مربوط به محصولات
- **apiClient** - کلاینت پایه برای درخواست‌های API

## توابع کمکی

- **format** - فرمت‌دهی اعداد، تاریخ و ارز
- **validation** - اعتبارسنجی ورودی‌ها
- **constants** - ثابت‌های برنامه

## توسعه

برای توسعه بیشتر، می‌توانید:

1. اتصال به API واقعی در فایل‌های `services/`
2. افزودن کامپوننت‌های جدید در پوشه `components/`
3. افزودن صفحات جدید در پوشه `app/`
4. سفارشی‌سازی استایل‌ها در `tailwind.config.ts`

## مجوز

این پروژه برای استفاده در پروژه‌های B2B طراحی شده است.


