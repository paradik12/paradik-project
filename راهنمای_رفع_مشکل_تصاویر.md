# راهنمای رفع مشکل نمایش تصاویر در پروژه Paradik

## مشکلات احتمالی و راه‌حل‌ها

### 1. مشکل اتصال اینترنت

تصاویر از سایت Unsplash استفاده می‌کنند. اگر اینترنت ندارید یا دسترسی به Unsplash محدود است:

**راه‌حل:**
- اتصال اینترنت را بررسی کنید
- اگر از VPN استفاده می‌کنید، مطمئن شوید که Unsplash در دسترس است

### 2. مشکل تنظیمات Next.js

اگر تصاویر هنوز نمایش داده نمی‌شوند:

**مراحل:**

1. **توقف سرور:**
   ```bash
   # در ترمینال Ctrl + C را بزنید
   ```

2. **پاک کردن کش Next.js:**
   ```bash
   # در PowerShell
   Remove-Item -Recurse -Force .next
   ```

   یا در Command Prompt:
   ```bash
   rmdir /s /q .next
   ```

3. **اجرای مجدد سرور:**
   ```bash
   npm run dev
   ```

### 3. بررسی Console مرورگر

1. مرورگر را باز کنید (F12)
2. به تب Console بروید
3. خطاهای مربوط به تصاویر را بررسی کنید

**خطاهای رایج:**
- `Image optimization failed` → مشکل تنظیمات Next.js
- `Failed to load resource` → مشکل اتصال اینترنت
- `CORS error` → مشکل دسترسی

### 4. استفاده از تصاویر محلی (جایگزین)

اگر مشکل ادامه داشت، می‌توانید از تصاویر محلی استفاده کنید:

**مراحل:**

1. **ایجاد پوشه public:**
   ```bash
   mkdir public
   mkdir public/images
   ```

2. **اضافه کردن تصاویر به public/images/**

3. **تغییر URL تصاویر در کامپوننت‌ها:**
   ```tsx
   // به جای:
   image: "https://images.unsplash.com/..."
   
   // استفاده کنید از:
   image: "/images/product-1.jpg"
   ```

### 5. غیرفعال کردن بهینه‌سازی تصاویر (موقت)

اگر هیچ کدام کار نکرد، می‌توانید بهینه‌سازی را غیرفعال کنید:

**در `next.config.ts`:**
```typescript
images: {
  unoptimized: true,
  // ... بقیه تنظیمات
}
```

⚠️ **توجه:** این راه‌حل فقط برای تست است و در production توصیه نمی‌شود.

### 6. بررسی فایل next.config.ts

مطمئن شوید که فایل `next.config.ts` به درستی تنظیم شده است:

```typescript
images: {
  remotePatterns: [
    {
      protocol: "https",
      hostname: "images.unsplash.com",
      pathname: "/**",
    },
  ],
}
```

### 7. بررسی Network Tab

1. مرورگر را باز کنید (F12)
2. به تب Network بروید
3. تصویری را در صفحه رفرش کنید
4. ببینید آیا درخواست تصویر ارسال می‌شود یا نه

**اگر درخواست ارسال نمی‌شود:**
- مشکل از کد است
- Console را برای خطاها بررسی کنید

**اگر درخواست ارسال می‌شود اما خطا می‌دهد:**
- مشکل اتصال یا CORS است
- URL تصویر را در تب Network بررسی کنید

## تست سریع

برای تست سریع، این URL را مستقیماً در مرورگر باز کنید:

```
https://images.unsplash.com/photo-1606312619070-d48b4bdc6e3c?w=400&h=400&fit=crop
```

اگر تصویر نمایش داده شد → مشکل از کد است
اگر تصویر نمایش داده نشد → مشکل از اینترنت یا دسترسی است

## راه‌حل پیشنهادی

1. ✅ ابتدا اتصال اینترنت را بررسی کنید
2. ✅ کش Next.js را پاک کنید (پوشه `.next`)
3. ✅ سرور را مجدداً اجرا کنید
4. ✅ Console مرورگر را برای خطاها بررسی کنید
5. ✅ اگر مشکل ادامه داشت، از تصاویر محلی استفاده کنید

## پشتیبانی

اگر مشکل حل نشد، لطفاً:
1. خطای Console را کپی کنید
2. تصویری از Network Tab بگیرید
3. نسخه Node.js را با `node --version` بررسی کنید

---

**موفق باشید! 🖼️**





