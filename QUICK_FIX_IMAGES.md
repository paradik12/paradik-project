# راه‌حل سریع مشکل تصاویر 🔧

## ⚡ راه‌حل فوری (2 دقیقه)

### مرحله 1: توقف سرور
در ترمینالی که سرور در حال اجرا است:
- **Ctrl + C** را بزنید

### مرحله 2: پاک کردن کش
در PowerShell اجرا کنید:
```powershell
.\fix-images.ps1
```

یا دستی:
```powershell
Remove-Item -Recurse -Force .next
```

### مرحله 3: راه‌اندازی مجدد
```bash
npm run dev
```

---

## 🔍 بررسی مشکلات احتمالی

### مشکل 1: اینترنت
✅ تست کنید:
```
https://images.unsplash.com/photo-1606312619070-d48b4bdc6e3c
```
اگر تصویر باز نشد → مشکل اینترنت یا فیلتر

### مشکل 2: Console خطا می‌دهد
1. F12 بزنید
2. تب Console را باز کنید
3. خطاها را بررسی کنید

خطاهای رایج:
- `Image optimization failed` → تنظیمات Next.js
- `Failed to load` → مشکل اتصال
- `CORS error` → مشکل دسترسی

### مشکل 3: تصاویر خالی نمایش داده می‌شود
✅ بررسی کنید:
- آیا background خاکستری می‌بینید؟ (تصویر در حال لود است)
- آیا خطای 404 می‌بینید؟

---

## ✅ راه‌حل‌های اضافی

### اگر مشکل ادامه داشت:

#### گزینه 1: استفاده از تصاویر محلی
1. پوشه `public/images` ایجاد کنید
2. تصاویر را در آن قرار دهید
3. URL را تغییر دهید: `/images/your-image.jpg`

#### گزینه 2: غیرفعال کردن بهینه‌سازی (موقت)
در `next.config.ts`:
```typescript
images: {
  unoptimized: true, // فقط برای تست
}
```

---

## 📞 بررسی سریع

در مرورگر این را تست کنید:
1. Console (F12) → آیا خطا دارید؟
2. Network (F12) → آیا درخواست تصویر ارسال می‌شود؟
3. Elements → آیا `<img>` یا `<Image>` وجود دارد؟

---

**موفق باشید!** 🚀








